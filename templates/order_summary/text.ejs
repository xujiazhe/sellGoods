

收件人:    <%= order.username %>,
手机:      <%= order.mobile %>,
地址:      <%= order.address %>,
大小:      <%= order.size %>,
颜色:      <%= order.color %>,
留言:      <%= order.gbook %>,
下单时间:  <%= order.date %>,
下单省份:  <%= order.ip %>,
产品id:    <%= order.id %>
